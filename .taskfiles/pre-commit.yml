---
version: "3"

tasks:
  init:
    desc: Initialize pre-commit hooks
    cmds:
      - pre-commit install-hooks
    silent: true

  run:
    desc: Run pre-commit
    cmds:
      - pre-commit run --all-files
    silent: true
